import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'elamana.online',
  appName: 'evangelism-forms-client',
  webDir: 'build'
};

export default config;
